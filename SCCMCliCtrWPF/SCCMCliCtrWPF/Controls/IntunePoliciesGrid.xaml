<UserControl x:Class="ClientCenter.Controls.IntunePoliciesGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid>
        <DockPanel>
            <ToolBar DockPanel.Dock="Top" Height="32">
                <Button Name="bt_Reload" Click="bt_Reload_Click" ToolTip="Reload applied MDM policy registry values (read-only)">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/SCCMCliCtrWPF;component/Images/shell32.dll_I0017_0409.ico" Height="16"/>
                        <TextBlock Margin="4,0,0,0" Text="Reload"/>
                    </StackPanel>
                </Button>
                <Separator/>
                <TextBlock Name="tbStatus" VerticalAlignment="Center" Margin="6,0,0,0" Text="Applied MDM policies (read-only)"/>
            </ToolBar>
            <DataGrid Name="dataGrid1" AutoGenerateColumns="False" IsReadOnly="True"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Section" Binding="{Binding Section}" MinWidth="120"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" MinWidth="160"/>
                    <DataGridTextColumn Header="Value" Binding="{Binding Value}" MinWidth="200" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>
